Simple quiz project using multiple Arduino sketches.

threebutsev:

Sketch which uses four buttons and three LEDs.
Three buttons are used for A, B, C selection on the LEDs.
One button is for RESET.

scorethree:

Sketch which uses buttons to add score and also to display the scores that have been added.
At the moment for three participants but can be scalable.